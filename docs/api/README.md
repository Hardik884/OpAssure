# API

Will contain the contract between frontend, backend, and ML:

- REST endpoint reference (FastAPI also serves generated OpenAPI docs at `/docs`)
- WebSocket message types and payload schemas
- Backend <-> ML service interfaces (inputs/outputs of each model)
- Versioning and error-handling conventions
