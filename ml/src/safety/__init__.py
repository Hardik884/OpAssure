"""Safety / risk intelligence models (seatbelt, proximity, incident risk).

Not implemented yet. This package will consume the shared feature pipeline
in `src.features.build_features` the same way `src.eta` and `src.anomaly`
do — see CLAUDE.md §11 for the reuse rule. No models yet, per this step's
scope (ML foundation only).
"""
