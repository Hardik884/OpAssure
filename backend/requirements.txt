fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
sqlalchemy>=2.0.30,<2.1
psycopg2-binary>=2.9.9,<3.0
python-dotenv>=1.0,<2.0

# Synthetic data. Faker is pinned exactly so operator names stay identical across installs.
numpy>=2.0,<3.0
Faker==40.39.0

# AI/ML layer (ml/): app/services/ml_bridge.py imports ml/ directly (sibling
# directory, see its own ml/requirements.txt) rather than installing it as a
# package — these three are ml/'s own runtime dependencies.
pandas>=2.2,<3.0
scikit-learn>=1.4,<2.0
joblib>=1.3,<2.0

# Tests
pytest>=8.0,<10.0
httpx>=0.27,<1.0
